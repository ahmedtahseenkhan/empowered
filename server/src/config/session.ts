/** Length of a regular lesson. Rates are quoted per session of this length. */
export const SESSION_MINUTES = 50;
/** Slots start on the hour; the remaining 10 minutes are a buffer between lessons. */
export const SLOT_STEP_MINUTES = 60;
